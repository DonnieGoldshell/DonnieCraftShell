# Data

This directory will contain verified and provisional data sets.

Recommended layout:

- `raw/` for source exports or imports.
- `normalized/` for application-ready data.
- `research/` for provisional notes marked `TODO / NEEDS VERIFICATION`.

Do not commit large generated dumps unless they are required for reproducible tests.
